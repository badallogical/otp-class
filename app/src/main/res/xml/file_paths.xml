<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- For sharing Excel from cacheDir -->
    <cache-path
        name="shared_cache"
        path="." />

    <!-- For sharing images from getExternalFilesDir(Pictures) -->
    <external-files-path
        name="external_files"
        path="Pictures" />

</paths>

